@model Web_Subasta.Models.ViewModels.SubastaViewModel
@{
    Layout = "_Layout";
    
    ViewData["Title"] = "Subastas Activas";
}
   
    <div class="container mt-5">
        <div class="row">
        @foreach (var subasta in Model.subastaListaDTO)
        {
            <div class="col-md-4">
                <div class="card mb-4">
                    <div class="card-body">
                        <h5 class="card-title">Subasta Activa: @subasta.titulo</h5>
                        <p class="card-text">Fecha de inicio: @subasta.fechaInicio.ToShortDateString()</p>
                        <p class="card-text">Fecha de fin: @subasta.fechaFinalizado.ToShortDateString()</p>
                        <p class="card-text">Fecha de fin: @subasta.estadoSubasta</p>
                        <a href="#" class="btn btn-primary">Participar en la subasta</a>
                    </div>
                </div>
            </div>
        }

        </div>
    </div>

